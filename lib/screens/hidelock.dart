import 'package:flutter/material.dart';

class Hidelock extends StatefulWidget {
  @override
  _HidelockState createState() => _HidelockState();
}

class _HidelockState extends State<Hidelock> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text('hidelock')),
      
    );
  }
}